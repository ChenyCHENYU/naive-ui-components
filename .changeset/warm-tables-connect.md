---
'@robot-admin/naive-ui-components': patch
---

Allow `C_Table` CRUD bindings to accept column refs produced by external data packages without requiring both packages to share an identical `TableColumn<T>` declaration.
